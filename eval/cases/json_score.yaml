id: "json-score"
query: >
  Return ONLY a JSON object with a single key "score" whose value is a number
  between 0.0 and 1.0 representing how confident you are that 2+2=4.
critical: true
checks:
  - type: json_path_numeric_range
    path: "score"
    min: 0.5
    max: 1.0

